services:
  frontend:
    build: ./frontend
    container_name: cash-register-frontend
    ports:
      - "4200:80" # Angular app accessible at http://localhost:4200
    depends_on:
      - backend

  backend:
    build: ./backend
    container_name: cash-register-backend
    ports:
      - "5000:5000"
    environment:
      - MONGO_URI=mongodb://mongodb:27017/mydb
    depends_on:
      - mongodb

  mongodb:
    image: mongo:6
    container_name: cash-register-db
    volumes:
      - mongo-data:/data/db

volumes:
  mongo-data:

# services:
#   # nginx:
#   #   image: nginx:alpine
#   #   ports:
#   #     - "4200:4200"
#   #   volumes:
#   #     - ./nginx.conf:/etc/nginx/nginx.conf:ro
#   #   depends_on:
#   #     - frontend

#   frontend:
#     build:
#       context: ./frontend
#     ports:
#       - '4200:4200'
#     container_name: selvetto-frontend

#     # depends_on:
#     #   - backend
